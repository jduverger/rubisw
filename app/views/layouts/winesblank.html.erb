<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Wines" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= yield :head %>
    </head>
  <body>
    <%= yield %>
  </body>
</html>
